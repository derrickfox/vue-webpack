// main.js
import { createApp } from 'vue';
import App from './App'; // Importing your root component, App.js in this case

// Create a new Vue application instance and mount it to the DOM
createApp(App).mount('#app');
